% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fixes.R
\name{patch_condn}
\alias{patch_condn}
\title{Patch condition number extraction}
\usage{
patch_condn(xpdb)
}
\arguments{
\item{xpdb}{An \code{xpose_data} or \code{xp_xtras} object.}
}
\value{
The \code{xpdb} object, with a corrected \code{'condn'} entry in
\code{xpdb$summary} (unchanged if \code{xpdb} is not from \code{nonmem}, or if
no eigenvalues could be found).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Bugfix for \code{xpose:::sum_condn}, the internal function \code{xpose} uses to
populate the \code{'condn'} (condition number) entry of \code{xpdb$summary}.

For NONMEM runs with more than one estimation method (e.g. \code{SAEM} followed
by importance sampling), the \code{.lst} file contains more than one
\verb{EIGENVALUES OF COR MATRIX OF ESTIMATE} block. \code{xpose} always uses the
\emph{first} block found, which is not necessarily from the final estimation
method, so the reported condition number can be wrong. This patch instead
uses the \emph{last} block, matching the value reported by NONMEM-adjacent
tools such as PsN's \code{sumo}.
}
\examples{
xpdb_ex_pk <- patch_condn(xpose::xpdb_ex_pk)

}
