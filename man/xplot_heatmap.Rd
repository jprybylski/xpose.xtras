% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xplot_heatmap.R
\name{xplot_heatmap}
\alias{xplot_heatmap}
\title{Generic heatmap plotting function}
\usage{
xplot_heatmap(
  xpdb,
  mat,
  digits = 3,
  limits = NULL,
  midpoint = 0,
  legend_name = "Value",
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  tag = NULL,
  plot_name = "heatmap",
  gg_theme,
  xp_theme,
  quiet,
  ...
)
}
\arguments{
\item{xpdb}{<\code{xp_xtras}> or <\code{xpose_data}> object. Used only for theming;
\code{mat} is plotted as-is.}

\item{mat}{A numeric matrix with row and column names. \code{NA} cells are
dropped (not plotted), which can be used to mask out redundant cells
(eg, the lower triangle of a symmetric matrix).}

\item{digits}{Number of significant digits to display in cell labels}

\item{limits}{Fill scale limits, as \code{c(low, high)}. Defaults to
\code{c(-1, 1) * max(abs(mat), na.rm = TRUE)}}

\item{midpoint}{Fill scale midpoint}

\item{legend_name}{Fill scale/legend title}

\item{title}{Plot title}

\item{subtitle}{Plot subtitle}

\item{caption}{Plot caption}

\item{tag}{Plot tag}

\item{plot_name}{Metadata name of plot}

\item{gg_theme}{As in \code{xpose}}

\item{xp_theme}{As in \code{xpose}}

\item{quiet}{Silence extra debugging output}

\item{...}{Additional aesthetics, passed to the \code{heatmap} tile layer and
\code{heatmaptxt} label layer (see \code{\link[=xp_xtra_theme]{xp_xtra_theme()}})}
}
\value{
The desired plot
}
\description{
Following the \code{xpose} design pattern, this is a generic template
(analogous to \code{\link[=xplot_boxplot]{xplot_boxplot()}} or \code{\link[=xplot_pairs]{xplot_pairs()}}) for rendering a
numeric matrix as a themed tile heatmap with cell value labels. It is not
tied to any particular data source; callers are expected to build a named
implementation on top of it (eg, \code{\link[=cormat]{cormat()}} for parameter
correlation/covariance matrices) rather than calling it directly for
everyday use.
}
\examples{
m <- matrix(c(1, 0.5, 0.5, 1), nrow = 2, dimnames = list(c("A", "B"), c("A", "B")))
xplot_heatmap(xpdb_x, m, quiet = TRUE)
}
