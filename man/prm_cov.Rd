% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xtra_pars.R
\name{prm_contcov}
\alias{prm_contcov}
\alias{prm_catcov}
\alias{prm_cov}
\title{Continuous/categorical covariate effect tables}
\usage{
prm_contcov(
  xpdb,
  ...,
  .problem = NULL,
  .subprob = NULL,
  .method = NULL,
  ci_method = c("simulation", "delta"),
  probs = c(0.05, 0.95),
  level = 0.95,
  nsim = 1000,
  quiet
)

prm_catcov(
  xpdb,
  ...,
  .problem = NULL,
  .subprob = NULL,
  .method = NULL,
  ci_method = c("simulation", "delta"),
  level = 0.95,
  nsim = 1000,
  quiet
)

prm_cov(
  xpdb,
  ...,
  .problem = NULL,
  .subprob = NULL,
  .method = NULL,
  ci_method = c("simulation", "delta"),
  probs = c(0.05, 0.95),
  level = 0.95,
  nsim = 1000,
  quiet
)
}
\arguments{
\item{xpdb}{<\code{xp_xtras}> object with associations declared via
\code{\link[=add_cov_association]{add_cov_association()}}}

\item{...}{<\code{\link[rlang:dyn-dots]{dynamic-dots}}> Optional \code{param ~ covariate}
selectors (bare, unquoted, same style as \code{\link[=drop_cov_association]{drop_cov_association()}}) to
restrict which declared associations are computed. Defaults to all of
them.}

\item{.problem}{<\code{numeric}> Problem number.}

\item{.subprob}{<\code{numeric}> Subprob number.}

\item{.method}{<\code{numeric}> Method.}

\item{ci_method}{<\code{character}> \code{"simulation"} (default) draws \code{nsim}
samples of each theta from \code{N(theta_hat, se)} (mirrors
\code{\link[=mutate_prm]{mutate_prm()}}'s \code{.autose} approach) and propagates them through the
(possibly nonlinear) effect_ratio function, taking the resulting sample
quantiles as the interval; most accurate for strongly nonlinear forms
(\code{power}, \code{exponential}, \code{hockey}). \code{"delta"} is a first-order analytic
(numerical-gradient) log-scale approximation -- cheap and
deterministic, but less accurate the more nonlinear the association is.
Both treat multiple thetas (eg \code{hockey}, multi-level \code{catshift}) as
independent, ignoring any covariance between them.}

\item{probs}{<\code{numeric(2)}> For \code{prm_contcov()}: quantiles of the
covariate's observed data used as the "low"/"high" evaluation points.}

\item{level}{<\code{numeric}> Confidence level for the effect interval.}

\item{nsim}{<\code{numeric}> Number of simulation draws, when
\code{ci_method = "simulation"}.}

\item{quiet}{Silence extra output.}
}
\value{
A \code{prm_cov_tbl} tibble (a plain tibble, classed for a future
print method) with one row per (parameter, covariate, evaluation point):
\code{param}, \code{covariate}, \code{covtype}, \code{level} (\code{"low"}/\code{"ref"}/\code{"high"} for
continuous, the raw category value for categorical), \code{value} (the
covariate value/level backing that row), \code{effect}, \code{ci_low}, \code{ci_high},
\code{ci_method}.
}
\description{
Computes, for each covariate association declared with
\code{\link[=add_cov_association]{add_cov_association()}}, the covariate's effect on the associated
parameter (as a ratio to the parameter's typical value, \code{1} at the
reference covariate value/level) at a handful of representative
evaluation points, with an uncertainty interval propagated from the
effect-size theta's standard error. This is what \code{\link[=xplot_forest]{xplot_forest()}}
plots; calling these directly is mostly useful for inspecting the
numbers before/without plotting.

\code{prm_contcov()} handles continuous covariates (\code{linear}, \code{power},
\code{exponential}, \code{hockey}, or \code{custom} associations), evaluated at the
low/reference/high points. \code{prm_catcov()} handles categorical
covariates (\code{catshift} or \code{custom} associations), evaluated at every
observed level. \code{prm_cov()} combines both.
}
\examples{

xpdb_x \%>\%
  add_cov_association(TVCL ~ power(CLCR, THETA7, ref = 64)) \%>\%
  prm_contcov()

xpdb_x \%>\%
  add_cov_association(TVCL ~ catshift(SEX, THETA4, ref = 1)) \%>\%
  prm_catcov()

xpdb_x \%>\%
  add_cov_association(
    TVCL ~ power(CLCR, THETA7, ref = 64),
    TVCL ~ catshift(SEX, THETA4, ref = 1)
  ) \%>\%
  prm_cov()

# Restrict to one association, and use the analytic delta-method CI
xpdb_x \%>\%
  add_cov_association(
    TVCL ~ power(CLCR, THETA7, ref = 64),
    TVCL ~ catshift(SEX, THETA4, ref = 1)
  ) \%>\%
  prm_cov(TVCL ~ CLCR, ci_method = "delta")

}
\seealso{
\code{\link[=add_cov_association]{add_cov_association()}}, \code{\link[=xplot_forest]{xplot_forest()}}
}
