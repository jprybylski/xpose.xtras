% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xtras_options.R
\name{set_xtras_options}
\alias{set_xtras_options}
\title{Set \code{xpose.xtras} session options}
\usage{
set_xtras_options(...)
}
\arguments{
\item{...}{<\code{\link[rlang:dyn-dots]{dynamic-dots}}> One or more of
\code{default_labs}, \code{default_watermark}, \code{save_dir}, \code{save_width},
\code{save_height}, \code{gg_theme}, \code{xp_theme}, given as \code{name = value}}
}
\value{
the previous values of the options that were set, invisibly
(see \code{\link[=options]{options()}})
}
\description{
Convenience wrapper around base \code{\link[=options]{options()}} for the \verb{xpose.xtras.*}
family of options recognized by this package. Prefixes are added
automatically and names are validated against the list below, so e.g.
\code{set_xtras_options(save_dir = "figures")} is equivalent to (but safer
against typos than) \code{options(xpose.xtras.save_dir = "figures")}.
Current values can be read back with regular \code{\link[=getOption]{getOption()}} (e.g.
\code{getOption("xpose.xtras.save_dir")}), or with \code{\link[=get_xtras_option]{get_xtras_option()}} for
\code{default_labs}/\code{default_watermark}, which also considers any
\code{xpdb}-level default.
}
\details{
Recognized options (all unset, i.e. \code{NULL}, by default):

\describe{
\item{\code{default_labs}}{Named list of default \code{title}/\code{subtitle}/
\code{caption}/\code{tag} templates (may contain \verb{@keyword} placeholders, see
\code{\link[xpose:parse_title]{xpose::parse_title()}}). Used by \code{\link[=apply_default_labs]{apply_default_labs()}} (and by
extension \code{\link[=ggsave_xp]{ggsave_xp()}}) as the lowest-precedence source for any
label a plot doesn't already have -- xpdb-level defaults (see
\code{\link[=set_default_labs]{set_default_labs()}}) and arguments passed directly to
\code{apply_default_labs()} both take precedence over this option.}
\item{\code{default_watermark}}{Named list of default \code{\link[=add_watermark]{add_watermark()}}
arguments (any of \code{label}/\code{colour}/\code{alpha}/\code{size}/\code{angle}/
\code{fontface}). Used by \code{add_watermark()} as the lowest-precedence
source -- xpdb-level defaults (see \code{\link[=set_default_watermark]{set_default_watermark()}}) and
arguments passed directly to \code{add_watermark()} both take precedence
over this option.}
\item{\code{save_dir}, \code{save_width}, \code{save_height}}{Defaults for the
\code{dir}/\code{width}/\code{height} arguments of \code{\link[=ggsave_xp]{ggsave_xp()}}, used whenever
those arguments aren't supplied explicitly.}
\item{\code{gg_theme}, \code{xp_theme}}{Default \code{ggplot2} theme / \code{xpose}
\code{xp_theme} (see \code{\link[xpose:update_themes]{xpose::update_themes()}}) applied to an \code{xpose_data}
object the first time it's converted via \code{\link[=as_xpdb_x]{as_xpdb_x()}},
so a project-wide look can be set once per session instead of
calling \code{\link[xpose:update_themes]{xpose::update_themes()}} on every \code{xpdb} individually. Has
no effect on \code{xpdb}s that are already \code{xp_xtras} objects.}
}
}
\examples{
set_xtras_options(save_dir = "figures", default_watermark = list(label = "DRAFT"))
getOption("xpose.xtras.save_dir")
set_xtras_options(save_dir = NULL, default_watermark = NULL)
}
