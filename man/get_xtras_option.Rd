% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xtras_options.R
\name{get_xtras_option}
\alias{get_xtras_option}
\title{Inspect which \code{xpose.xtras} option value is dominant}
\usage{
get_xtras_option(name, xpdb = NULL)
}
\arguments{
\item{name}{<\code{character}> One of the option names recognized by
\code{\link[=set_xtras_options]{set_xtras_options()}}}

\item{xpdb}{<\code{\link[xpose:xpose_data]{xpose_data}}> or <\code{xp_xtras}> object
to check for an \code{xpdb}-level value (optional; only consulted for
\code{default_labs}/\code{default_watermark})}
}
\value{
a list with elements \code{option} (the \code{\link[=getOption]{getOption()}} value),
\code{xpdb} (the \code{xpdb}-level value, or \code{NULL}), and \code{dominant} (one of
\code{"option"}, \code{"xpdb"}, or \code{"neither"})
}
\description{
For the two "two-tier" options -- \code{default_labs} and
\code{default_watermark}, which can be set both as a session-wide R option
(via \code{\link[=set_xtras_options]{set_xtras_options()}}) and per-\code{xpdb} (via \code{\link[=set_default_labs]{set_default_labs()}} /
\code{\link[=set_default_watermark]{set_default_watermark()}}) -- reports the value at each tier and which
one is dominant, i.e. would currently be used by \code{\link[=apply_default_labs]{apply_default_labs()}}
/ \code{\link[=add_watermark]{add_watermark()}} absent any argument passed directly to those
functions (which always takes precedence over both tiers).

The other options recognized by \code{\link[=set_xtras_options]{set_xtras_options()}} (\code{save_dir},
\code{save_width}, \code{save_height}, \code{gg_theme}, \code{xp_theme}) have no \code{xpdb}-level
tier to compare against (see the Details in \code{\link[=set_xtras_options]{set_xtras_options()}}), so
for those \code{xpdb} is always \code{NULL} and \code{dominant} is \code{"option"} or
\code{"neither"}.
}
\examples{
options(xpose.xtras.default_labs = list(caption = "session default"))
xpdb_x2 <- set_default_labs(xpdb_x, caption = "model-specific")
get_xtras_option("default_labs", xpdb_x2)
options(xpose.xtras.default_labs = NULL)
}
