% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colinearity.R
\name{get_cov_matrix}
\alias{get_cov_matrix}
\title{Extract a parameter covariance or correlation matrix}
\usage{
get_cov_matrix(
  xpdb,
  type = c("correlation", "covariance"),
  .problem = NULL,
  .subprob = NULL,
  .method = NULL,
  drop_fixed = TRUE,
  quiet
)
}
\arguments{
\item{xpdb}{<\code{xp_xtras}> or <\code{xpose_data}> object}

\item{type}{<\code{character}> Either \code{"correlation"} (default) or \code{"covariance"}}

\item{.problem}{<\code{numeric}> Problem number to use. Uses the xpose default
if not provided. Ignored for \code{nlmixr2} models.}

\item{.subprob}{<\code{numeric}> Subproblem number to use. Uses the xpose
default if not provided. Ignored for \code{nlmixr2} models.}

\item{.method}{<\code{character}> Method to use. Uses the xpose default if not
provided. Ignored for \code{nlmixr2} models.}

\item{drop_fixed}{<\code{logical}> Drop fixed (or otherwise not estimated)
parameters from the matrix. See Details.}

\item{quiet}{<\code{logical}> Silence extra debugging output}
}
\value{
A symmetric numeric matrix, with parameter names as \code{dimnames}.
}
\description{
Pulls the uncertainty (covariance step) matrix for estimated parameters,
with built-in support for \code{nonmem} (via the \code{.cov}/\code{.cor} output tables)
and \code{nlmixr2} (via the fit object's covariance matrix) models. This is
what feeds \code{\link[=cormat]{cormat()}}, but is exported separately since it may be
useful on its own (eg, for programmatic checks on parameter colinearity).
}
\details{
For \code{nonmem} models, the matrix is built from the \code{.cor}/\code{.cov} output
tables produced by the \verb{$COV} step. NONMEM includes fixed-effect
parameters in these tables as placeholder zeros (since no uncertainty is
estimated for them); \code{drop_fixed} (the default) removes them.

For \code{nlmixr2} models, uncertainty is only calculated for fixed-effect
(\code{theta}) parameters; \code{nlmixr2} does not report standard errors, and
therefore no covariance/correlation, for random-effect (\code{omega})
elements. \code{drop_fixed} has no effect here, since parameters without a
standard error are already excluded from the fit's covariance matrix.

In both cases, if the covariance step was not run, or did not complete
successfully, an informative error is raised rather than returning
partial or placeholder data.
}
\examples{
get_cov_matrix(xpdb_x)
get_cov_matrix(xpdb_x, type = "covariance")

\donttest{
xpdb_nlmixr2 <- nlmixr_example("xpdb_nlmixr2")
get_cov_matrix(xpdb_nlmixr2)
}
}
