% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xtra_plot_all.R
\name{plot.xpose_data}
\alias{plot.xpose_data}
\title{Generate a batch of diagnostic plots from an \code{xpdb}}
\usage{
\method{plot}{xpose_data}(x, y, plots, ..., force = FALSE, quiet)
}
\arguments{
\item{x}{<\code{\link[xpose:xpose_data]{xpose_data}}> or <\code{xp_xtras}> object}

\item{y}{unused; present only for consistency with the
\code{\link[graphics:plot]{graphics::plot()}} generic}

\item{plots}{A list of plot specs (see Description); defaults to the
resolved value described in Details}

\item{...}{unused}

\item{force}{<\code{logical}> If \code{FALSE} (the default), a failing plot
immediately aborts the whole call. If \code{TRUE}, a failing plot is instead
skipped (with a warning), and the rest of \code{plots} is still attempted.}

\item{quiet}{<\code{logical}> Silence the loading spinner and the summary
warning issued when \code{force = TRUE} and at least one plot failed;
defaults to \code{x$options$quiet}}
}
\value{
A flat, named list of \code{ggplot}/\code{xpose_plot} objects
}
\description{
Runs a list of plot-generating calls against \code{x} in one go, returning a
flat, named list of the results. Each element of \code{plots} (or the
resolved default, see Details) is either:
\itemize{
\item a bare function taking a single \code{xpdb} argument, or
\item a one-sided formula in the \code{~ fn(.x)} idiom used elsewhere in this
package (see \code{\link[=focus_function]{focus_function()}}), letting extra arguments be baked
straight into the call -- e.g. \code{~ xpose::res_vs_idv(.x, res = "CWRES")}.
}

Both forms are converted to callables with \code{\link[purrr:as_mapper]{purrr::as_mapper()}}. Because
entries are just calls, the same underlying plot function can appear
more than once with different options (e.g. \code{res_vs_idv} for both CWRES
and IWRES) -- see the examples.

If a single entry itself returns a list of plots (as e.g. \code{eta_grid()}
can, given more than one eta), that list is flattened into the overall
output rather than kept as a nested list -- so the return value is
always a flat list of \code{ggplot}/\code{xpose_plot} objects.
}
\details{
\code{plots} is resolved, in increasing precedence, from: this package's
built-in default (\code{dv_vs_ipred}, \code{dv_vs_pred}, \code{res_vs_idv},
\code{res_vs_pred}, \code{eta_distrib}, \code{eta_grid}, \code{eta_vs_cov_grid},
\code{ind_plots_sample}), the \code{xpose.xtras.default_plots} session option, an
\code{xpdb}-level default set via \code{\link[=set_default_plots]{set_default_plots()}}, and finally the
\code{plots} argument itself, if supplied.

A loading spinner is shown (interactive sessions only, unless
\code{quiet = TRUE}) while plots are generated. If a plot fails to generate,
the default (\code{force = FALSE}) is to immediately raise an error (showing
the original error as its parent) without returning any plots at all.
With \code{force = TRUE}, a failure is instead emitted as a warning and that
entry is skipped, so the rest of \code{plots} still gets a chance to run.
}
\examples{
\donttest{
# the package's built-in default battery of diagnostic plots
default_plots <- plot(xpdb_x, quiet = TRUE)
names(default_plots)

# a custom spec: bare functions, formulas, and the same function twice
custom_plots <- plot(
  xpdb_x,
  plots = list(
    xpose::dv_vs_ipred,
    ~ xpose::res_vs_idv(.x, res = "CWRES"),
    ~ xpose::res_vs_idv(.x, res = "IWRES")
  ),
  quiet = TRUE
)
names(custom_plots)
}
}
