% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colinearity.R
\name{cormat}
\alias{cormat}
\title{Parameter correlation/covariance matrix heatmap}
\usage{
cormat(
  xpdb,
  type = c("correlation", "covariance"),
  .problem = NULL,
  .subprob = NULL,
  .method = NULL,
  drop_fixed = TRUE,
  digits,
  title,
  subtitle = "Ofv: @ofv, Condition number: @condn",
  caption = "@dir",
  tag = NULL,
  quiet,
  ...
)
}
\arguments{
\item{xpdb}{<\code{xp_xtras}> or <\code{xpose_data}> object}

\item{type}{<\code{character}> Either \code{"correlation"} (default) or \code{"covariance"}}

\item{.problem}{<\code{numeric}> Problem number to use. Uses the xpose default if not provided.}

\item{.subprob}{<\code{numeric}> Subproblem number to use. Uses the xpose default if not provided.}

\item{.method}{<\code{character}> Method to use. Uses the xpose default if not provided.}

\item{drop_fixed}{<\code{logical}> Passed to \code{\link[=get_cov_matrix]{get_cov_matrix()}}}

\item{digits}{Number of significant digits to display in cell labels. Defaults to \code{\link[=reportable_digits]{reportable_digits()}}}

\item{title}{Plot title}

\item{subtitle}{Plot subtitle}

\item{caption}{Plot caption}

\item{tag}{Plot tag}

\item{quiet}{Silence extra debugging output}

\item{...}{Additional aesthetics, passed to \code{\link[=xplot_heatmap]{xplot_heatmap()}}}
}
\value{
The desired plot
}
\description{
Visualizes the parameter correlation (or covariance) matrix as a heatmap,
filling a gap left behind in translation from \code{xpose4}. Values come from
\code{\link[=get_cov_matrix]{get_cov_matrix()}}, which has built-in support for \code{nonmem} and
\code{nlmixr2} models; rendering is done with the generic \code{\link[=xplot_heatmap]{xplot_heatmap()}}
template.
}
\details{
Only the upper triangle of the matrix is drawn, as it is symmetric.
Fixed-effect (\code{theta}) and random-effect (\code{omega}/\code{sigma}) parameters are
both included for \code{nonmem} models, when available and not fixed. For
\code{nlmixr2} models, only fixed effects are included, as \code{nlmixr2} does not
report uncertainty for random effects. See \code{\link[=get_cov_matrix]{get_cov_matrix()}} for
further details on availability; if the covariance step was not run, or
did not complete successfully, an informative error is raised.
}
\examples{
cormat(xpdb_x)
cormat(xpdb_x, type = "covariance")
}
