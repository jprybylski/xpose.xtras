% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{recalc_shk}
\alias{recalc_shk}
\title{Recalculate eta shrinkage from individual estimates}
\usage{
recalc_shk(
  xpdb,
  ...,
  .etastype = 1,
  .problem = NULL,
  .subprob = NULL,
  .method = NULL,
  drop_fixed = TRUE,
  quiet
)
}
\arguments{
\item{xpdb}{<\code{xpose_data}\link[xpose:xpose_data]{xpose::xpose_data}> or \code{xp_xtras} object}

\item{...}{<\code{tidyselect}> Which eta column(s) to recalculate shrinkage
for. Defaults to every \code{eta} column for \code{.problem}.}

\item{.etastype}{<\code{numeric(1)}> \code{1} (the default) excludes, for each eta,
individuals whose estimate is a "true zero" (exactly \code{0}, as opposed to
merely shrunk near it) from the calculation; \code{0} keeps them. See Details.}

\item{.problem}{<\code{numeric}> Problem number to use. Uses the xpose default if not provided.}

\item{.subprob}{<\code{numeric}> Subproblem number to use. Uses the xpose default if not provided.}

\item{.method}{<\code{character}> Method to use. Uses the xpose default if not provided.}

\item{drop_fixed}{<\code{logical}> Drop fixed etas (which have no meaningful
shrinkage to recalculate), as in \code{\link[xpose:drop_fixed_cols]{xpose::drop_fixed_cols}}.}

\item{quiet}{<\code{logical}> Silence extra debugging output}
}
\value{
A tibble with one row per eta, reporting the omega used, the
number of individuals excluded (if any), and the recalculated
shrinkage (as a percentage, to stay consistent with \code{\link[=get_shk]{get_shk()}}).
}
\description{
Unlike \code{\link[=get_shk]{get_shk()}}, which parses the eta shrinkage NONMEM itself
reported in the output file, this recalculates shrinkage directly from
the individual (empirical Bayes) eta estimates found in the data, using
the standard \eqn{100 \times (1 - SD(\eta)/\omega)} formula, where
\eqn{\omega} is the standard deviation implied by the associated
diagonal omega estimate.
}
\details{
An eta is a "true zero" for an individual when NONMEM never had grounds
to move it away from its prior mean of \code{0}, eg an individual with no
observations contributing to the objective function. That is different
from an eta that is merely shrunk close to \code{0} through legitimate
estimation, and including "true zero" individuals in the shrinkage
calculation biases it, since they carry no information about the actual
empirical distribution of etas. \code{.etastype = 1} (the default) excludes
them from the calculation; \code{.etastype = 0} reproduces the traditional,
unadjusted calculation.
}
\examples{
recalc_shk(xpdb_x)

# Just a subset of etas...
recalc_shk(xpdb_x, ETA1)

# Including "true zero" etas in the calculation
recalc_shk(xpdb_x, .etastype = 0)

}
