% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{normalize_etas}
\alias{normalize_etas}
\alias{normalise_etas}
\title{Normalize etas by their omega- or empirical-SD-implied scale}
\usage{
normalize_etas(
  xpdb,
  ...,
  .use_sd = FALSE,
  .problem = NULL,
  .subprob = NULL,
  .method = NULL,
  quiet
)

normalise_etas(
  xpdb,
  ...,
  .use_sd = FALSE,
  .problem = NULL,
  .subprob = NULL,
  .method = NULL,
  quiet
)
}
\arguments{
\item{xpdb}{<\code{xpose_data}\link[xpose:xpose_data]{xpose::xpose_data}> or \code{xp_xtras} object}

\item{...}{<\code{tidyselect}> Which eta column(s) to (re)compute a
normalization factor for. Defaults to every \code{eta} column for
\code{.problem}.}

\item{.use_sd}{<\code{logical}> If \code{TRUE}, normalize by the empirical
standard deviation of each eta's individual estimates instead of the
omega-implied one; see Details. Defaults to \code{FALSE}.}

\item{.problem}{<\code{numeric}> Problem number to use. Uses the xpose default if not provided.}

\item{.subprob}{<\code{numeric}> Subproblem number to use. Uses the xpose default if not provided.}

\item{.method}{<\code{character}> Method to use. Uses the xpose default if not provided.}

\item{quiet}{<\code{logical}> Silence extra debugging output}
}
\value{
\code{xp_xtras} object, with \code{normalize_etas} set under \verb{$options}
}
\description{
Sets an \code{xpdb}-level option (\code{normalize_etas}, see \code{\link[=set_option]{set_option()}})
consumed by \code{\link[=eta_grid]{eta_grid()}}/\code{\link[=eta_vs_cov_grid]{eta_vs_cov_grid()}}/\code{\link[=eta_vs_contcov]{eta_vs_contcov()}}/
\code{\link[=eta_vs_catcov]{eta_vs_catcov()}}: each selected eta is divided by its typical scale
-- by default the standard deviation implied by its associated
diagonal omega estimate (\code{sqrt(omega)}), same as \code{\link[=recalc_shk]{recalc_shk()}} uses
-- before being plotted, so etas modeled on very different scales
(eg a normally-distributed eta next to a log-normal one with a much
larger omega) can be compared on one shared plot without the
larger-scale eta dominating.

\code{normalise_etas()} is an alias, for the British/rest-of-world spelling.
}
\details{
This only ever affects how those four plotting functions \emph{display}
etas -- it never modifies \code{xpdb$data}, so \code{\link[xpose:get_data]{get_data()}}
and every other consumer of the eta columns keep seeing the raw
(unnormalized) values.

The default (omega-based) scale relies on the same internal
name/numbering match between eta columns and diagonal omega estimates
(see \code{\link[=recalc_shk]{recalc_shk()}}'s Details for when that can fail, eg unconventional
eta naming that isn't a \code{nlmixr2}-style direct match to a parameter
table \code{name} and also doesn't follow NONMEM's \verb{ETA<k>}/\code{ETA(k)}
numbering). When that match fails, or there simply is no reliable
omega for these etas (eg a hand-built or simulated \code{xpdb}),
\code{.use_sd = TRUE} sidesteps it entirely, scaling by the empirical
standard deviation of each eta's own individual estimates instead --
at the cost of that scale itself being sample-dependent (and shrinkage-
deflated) rather than reflecting the model's estimated random-effect
variance.

Calling \code{normalize_etas()} again merges into (rather than replacing)
any previously-set factors -- via \code{\link[=set_option]{set_option()}}'s
\code{\link[utils:modifyList]{utils::modifyList()}} merge -- so \code{...} can be used to (re)compute
just a subset of etas, eg after refitting. To turn normalization off
again, either for specific etas (\code{set_option(xpdb, normalize_etas = list(ETA1 = NULL))}) or entirely (\code{set_option(xpdb, normalize_etas = NULL)}), call \code{\link[=set_option]{set_option()}} directly.
}
\examples{
xpdb_norm <- normalize_etas(xpdb_x)
eta_grid(xpdb_norm)

# Just a subset of etas...
normalize_etas(xpdb_x, ETA1)

# By empirical SD instead, eg if the omega match fails or is unreliable
normalize_etas(xpdb_x, .use_sd = TRUE)

}
\seealso{
\code{\link[=recalc_shk]{recalc_shk()}}, which uses the same omega-matching logic
}
