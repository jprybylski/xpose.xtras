% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xplot_forest.R
\name{xplot_forest}
\alias{xplot_forest}
\title{Default xpose forest plot function}
\usage{
xplot_forest(
  xpdb,
  mapping = NULL,
  type = "pi",
  orientation = "y",
  xscale = "continuous",
  yscale = "discrete",
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  tag = NULL,
  plot_name = "forest",
  gg_theme,
  xp_theme,
  opt,
  quiet,
  ...
)
}
\arguments{
\item{xpdb}{<\code{xp_xtras}> or <\code{xpose_data}> object}

\item{mapping}{\code{ggplot2} style mapping. Expected aesthetics: \code{x}/\code{y}
(the point) and \code{xmin}/\code{xmax} (the interval), or the mirrored roles
when \code{orientation = "x"}.}

\item{type}{See Details.}

\item{orientation}{Defaults to \code{'y'} (categories on the y-axis, values
on the x-axis -- the conventional forest-plot layout).}

\item{xscale}{Defaults to \code{'continuous'}.}

\item{yscale}{Defaults to \code{'discrete'}.}

\item{title}{Plot title}

\item{subtitle}{Plot subtitle}

\item{caption}{Plot caption}

\item{tag}{Plot tag}

\item{plot_name}{Metadata name of plot}

\item{gg_theme}{As in \code{xpose}}

\item{xp_theme}{As in \code{xpose}}

\item{opt}{Processing options for fetched data}

\item{quiet}{Silence extra debugging output}

\item{...}{Any additional aesthetics, or overrides for the reference
line (eg \code{vline_xintercept = 1} for a ratio-style forest plot; defaults
to \code{0} like the rest of the package's guide lines, see
\code{\link[=xp_xtra_theme]{xp_xtra_theme()}}).}
}
\value{
The desired plot
}
\description{
Manually generate a forest-style plot (point + interval per category)
from an xpdb object. This is a generic, low-level renderer in the same
spirit as \code{\link[=xplot_boxplot]{xplot_boxplot()}}/\code{\link[xpose:xplot_scatter]{xpose::xplot_scatter()}}: it has no built-in knowledge
of covariate associations, \code{\link[=prm_cov]{prm_cov()}}, or any other specific data
source -- it just draws a point + interval (and, per \code{type}, a
reference guide line) from whatever \code{mapping}/pre-fetched \code{opt} data
it's given. See \code{\link[=cov_forest]{cov_forest()}} for the covariate-association-specific
wrapper that prepares that mapping from \code{\link[=prm_cov]{prm_cov()}} and calls this
function to render it.
}
\details{
For type-based customization of plots:
\itemize{
\item \code{p} point (from \code{geom_point}) -- the effect estimate
\item \code{i} interval (from \code{geom_linerange}) -- the confidence/credible
interval
\item \code{l} reference line through the theme's \code{vline_xintercept}/
\code{hline_yintercept} (\code{0} by default; a ratio-style forest plot will
typically override this to \code{1}, see \code{\link[=cov_forest]{cov_forest()}})
}

A violin/density layer (eg showing the simulation draws behind an
interval) is a natural future addition to this function but is not yet
implemented -- the data shape it needs (one row per draw, rather than
one row per category) is different enough from the point/interval
layers' shape that it needs its own design pass rather than being
bolted on.
}
