% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xtra_labs.R
\name{apply_default_labs}
\alias{apply_default_labs}
\title{Apply default label overrides to a plot}
\usage{
apply_default_labs(plot, ..., xpdb = NULL, overwrite = FALSE)
}
\arguments{
\item{plot}{<\code{ggplot}> or <\code{xpose_plot}> object}

\item{...}{<\code{\link[rlang:dyn-dots]{dynamic-dots}}> Direct overrides for
\code{title}/\code{subtitle}/\code{caption}/\code{tag}, taking precedence over both the
option- and \code{xpdb}-level defaults}

\item{xpdb}{<\code{\link[xpose:xpose_data]{xpose_data}}> or <\code{xp_xtras}> object
\code{plot} was built from, used to resolve \code{xpdb}-level defaults (see
\code{\link[=set_default_labs]{set_default_labs()}}) and to resolve \verb{@keyword} placeholders in any of
the labels. If omitted and \code{plot} is an \code{xpose_plot}, \verb{@keyword}
placeholders are still resolved (using the reduced context xpose
attaches to the plot) but \code{xpdb}-level defaults are not available}

\item{overwrite}{<\code{logical}> Replace labels already present on \code{plot}
(default \code{FALSE}, meaning only missing labels are filled in)}
}
\value{
\code{plot}, with resolved labels applied
}
\description{
Resolves \code{title}/\code{subtitle}/\code{caption}/\code{tag} labels for \code{plot} from (in
increasing precedence):
\enumerate{
\item the \code{xpose.xtras.default_labs} R option (a named list, see examples),
\item defaults set on \code{xpdb} via \code{\link[=set_default_labs]{set_default_labs()}}, when \code{xpdb} is
supplied (a rendered plot does not retain enough of its source
\code{xpdb} to look this up automatically -- pass it explicitly),
\item values passed directly via \code{...}.
}

By default only labels not already set on the plot are filled in; set
\code{overwrite = TRUE} to replace existing labels too. This is a standalone
function rather than something wired into \code{print.xpose_plot()}, so it
works the same regardless of whether that print method still exists
(see issue #36).
}
\examples{
options(xpose.xtras.default_labs = list(caption = "Draft -- do not distribute"))
p <- xpose::dv_vs_ipred(xpose::xpdb_ex_pk)
apply_default_labs(p)
options(xpose.xtras.default_labs = NULL)
}
