% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xplot_binned.R
\name{xplot_binned}
\alias{xplot_binned}
\title{Generic binned trend plotting function}
\usage{
xplot_binned(
  xpdb,
  mapping = NULL,
  group = "variable",
  type = "pl",
  xscale = "discrete",
  yscale = "continuous",
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  tag = NULL,
  plot_name = "binned",
  gg_theme,
  xp_theme,
  opt,
  quiet,
  ...
)
}
\arguments{
\item{xpdb}{<\code{xp_xtras}> or <\code{xpose_data}> object}

\item{mapping}{\code{ggplot2} style mapping. Expected to include at least \code{x}
and \code{y}.}

\item{group}{Column name distinguishing multiple summarized series (eg
a \code{variable} column). Points/lines/smooths are both connected and
coloured by this column (see \code{\link[xpose:xplot_scatter]{xpose::xplot_scatter()}} for the same
connecting-line convention).}

\item{type}{String setting the type of plot to be used: point \code{p}, line
\code{l}, and smooth \code{s}, or any combination thereof.}

\item{xscale}{Defaults to \code{discrete}.}

\item{yscale}{Defaults to \code{continuous}.}

\item{title}{Plot title}

\item{subtitle}{Plot subtitle}

\item{caption}{Plot caption}

\item{tag}{Plot tag}

\item{plot_name}{Metadata name of plot}

\item{gg_theme}{As in \code{xpose}}

\item{xp_theme}{As in \code{xpose}}

\item{opt}{Processing options for fetched data}

\item{quiet}{Silence extra debugging output}

\item{...}{Additional aesthetics, passed to the \code{point}, \code{line} and
\code{smooth} layers.}
}
\value{
The desired plot
}
\description{
Following the \code{xpose} design pattern, this is a generic template
(analogous to \code{\link[=xplot_boxplot]{xplot_boxplot()}} or \code{\link[=xplot_pairs]{xplot_pairs()}}) for rendering
already-summarized/binned data as connected points and/or lines across
a (typically discrete, possibly ordered) x variable. It is not tied to
any particular binning or summarization logic -- callers are expected
to shape their data (eg via \code{opt}'s \code{post_processing}) and build a named
implementation on top of it (eg \code{\link[=catdv_vs_occ]{catdv_vs_occ()}}) rather than calling
it directly for everyday use.
}
\details{
Unlike \code{\link[xpose:xplot_scatter]{xpose::xplot_scatter()}}'s raw per-subject spaghetti plots,
\code{xplot_binned()} assumes the supplied data is already one row per
x/group combination (eg per occasion, per series) -- it does no binning,
aggregation, or tidying of its own.
}
