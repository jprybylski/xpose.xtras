% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bbr.R
\name{xp_from_bbr}
\alias{xp_from_bbr}
\title{Convenience function for ingesting a bbr model into xpose and xpose.xtras}
\usage{
xp_from_bbr(.mod, ..., .use_bbr_descr = TRUE)
}
\arguments{
\item{.mod}{<\code{bbi_nonmem_model}> A \code{bbr} NONMEM model object, e.g. as
returned by \code{\link[bbr:read_model]{bbr::read_model()}}.}

\item{...}{Passed to \code{\link[=xtras_data]{xtras_data()}} (and, in turn, \code{\link[xpose:xpose_data]{xpose::xpose_data()}}).}

\item{.use_bbr_descr}{<\code{logical}> If \code{TRUE} (default) and \code{.mod} carries a
\code{bbr} description, use it to set the \code{descr} property of the result (see
\code{\link[=set_prop]{set_prop()}}), taking precedence over any description parsed from the
NONMEM output itself.}
}
\value{
An <\code{xp_xtras}> object
}
\description{
\href{https://metrumresearchgroup.github.io/bbr/}{bbr} is a Metrum Research
Group package for managing NONMEM modeling workflows via \code{bbi}. It is not
distributed on CRAN (see \code{Additional_repositories} in this package's
\code{DESCRIPTION} for where to obtain it).

Reading a \code{bbr}-managed model into \code{xpose}/\code{xpose.xtras} normally requires
manually reconstructing the output file path from the model object, e.g.

\preformatted{
xtras_data(
  file = file.path(bbr::get_output_dir(mod), paste0(bbr::get_model_id(mod), ".lst"))
)
}

\code{xp_from_bbr()} wraps that pipeline.
}
\examples{
if (requireNamespace("bbr", quietly = TRUE)) {
  # Build a bbr-style model directory from the bundled pheno_saemimp example
  src_dir <- system.file("pheno_saemimp", package = "xpose.xtras")
  mod_dir <- tempfile("xp_from_bbr_ex")
  dir.create(mod_dir)
  file.copy(file.path(src_dir, "run18.mod"), file.path(mod_dir, "18.mod"))
  out_dir <- file.path(mod_dir, "18")
  dir.create(out_dir)
  out_files <- setdiff(list.files(src_dir, pattern = "^run18\\\\."), "run18.mod")
  for (f in out_files) {
    file.copy(
      file.path(src_dir, f),
      file.path(out_dir, paste0("18.", sub("^run18\\\\.", "", f)))
    )
  }
  # bbr considers a run finished once bbi has written this file
  writeLines("{}", file.path(out_dir, "bbi_config.json"))

  mod <- bbr::new_model(file.path(mod_dir, "18"), .description = "Phenobarbital SAEM model")
  xp_from_bbr(mod)
}
}
\seealso{
\code{\link[bbr:read_model]{bbr::read_model()}}, \code{\link[=xtras_data]{xtras_data()}}
}
