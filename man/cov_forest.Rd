% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariates.R
\name{cov_forest}
\alias{cov_forest}
\title{Covariate effect forest plot}
\usage{
cov_forest(
  xpdb,
  ...,
  log = "x",
  forest_opts = list(),
  title = "Covariate effects on model parameters | @run",
  subtitle = "Ratio to typical parameter value; reference line at 1",
  caption = "@dir",
  tag = NULL,
  .problem = NULL,
  .subprob = NULL,
  .method = NULL,
  quiet
)
}
\arguments{
\item{xpdb}{<\code{xp_xtras}> object with covariate associations declared
via \code{\link[=add_cov_association]{add_cov_association()}}}

\item{...}{<\code{\link[rlang:dyn-dots]{dynamic-dots}}> Forwarded to
\code{\link[=prm_cov]{prm_cov()}} -- eg \code{param ~ covariate} selectors, \code{ci_method},
\code{probs}, \code{level}, \code{nsim}.}

\item{log}{Passed to \code{\link[xpose:check_scales]{xpose::check_scales()}} for the x-axis; \code{"x"}
(default) log-scales the effect-ratio axis, \code{NULL} disables it.}

\item{forest_opts}{<\code{list}> Extra named arguments forwarded to
\code{\link[=xplot_forest]{xplot_forest()}} (eg \code{type}, or theme overrides), the same way
\code{pairs_opts} works for \code{\link[=cov_grid]{cov_grid()}}/\code{\link[=eta_grid]{eta_grid()}}.}

\item{title}{Plot title}

\item{subtitle}{Plot subtitle}

\item{caption}{Plot caption}

\item{tag}{Plot tag}

\item{.problem}{<\code{numeric}> Problem number}

\item{.subprob}{<\code{numeric}> Subprob number}

\item{.method}{<\code{numeric}> Method}

\item{quiet}{Silence extra output}
}
\value{
The desired plot
}
\description{
Visualizes the effect of covariates on structural parameters, as
declared with \code{\link[=add_cov_association]{add_cov_association()}}, as a forest plot: one row per
(parameter, covariate, evaluation point), the point estimate and
interval as a ratio to the parameter's typical value, with a reference
line at \code{1}.

This is the covariate-specific wrapper: it calls \code{\link[=prm_cov]{prm_cov()}} to
compute the effect-size table and \code{\link[=xplot_forest]{xplot_forest()}} (a generic,
forest-plot-agnostic renderer, see its own documentation) to draw it.
}
\examples{
\donttest{

xpdb_x \%>\%
  add_cov_association(
    TVCL ~ power(CLCR, THETA7, ref = 64),
    TVCL ~ catshift(SEX, THETA4, ref = 1)
  ) \%>\%
  cov_forest()
}
}
\seealso{
\code{\link[=add_cov_association]{add_cov_association()}}, \code{\link[=prm_cov]{prm_cov()}}, \code{\link[=xplot_forest]{xplot_forest()}}
}
