% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fixes.R
\name{left_join_x}
\alias{left_join_x}
\alias{left_join.xpose_data}
\title{Backfill missing variables via a left join}
\usage{
left_join_x(
  x,
  y,
  by = NULL,
  copy = FALSE,
  suffix = c(".x", ".y"),
  ...,
  keep = NULL,
  .problem = NULL
)

\method{left_join}{xpose_data}(
  x,
  y,
  by = NULL,
  copy = FALSE,
  suffix = c(".x", ".y"),
  ...,
  keep = NULL,
  .problem = NULL
)
}
\arguments{
\item{x}{An \code{xpose_data} or \code{xp_xtras} object.}

\item{y}{A data frame (or another object coercible to one) to join in.}

\item{by}{Join specification, as in <\code{\link[dplyr:left_join]{dplyr::left_join}}>. If \code{NULL}, a natural join is performed using variables common to \code{x} and \code{y}.}

\item{copy}{If \code{x} and \code{y} are not from the same source and \code{copy = TRUE}, \code{y} is copied to bring it into the same source as \code{x}. See <\code{\link[dplyr:left_join]{dplyr::left_join}}>.}

\item{suffix}{Suffixes used internally to disambiguate a column shared by \code{x} and \code{y} before it is backfilled into a single column; not visible in the result.}

\item{...}{Other parameters passed onto <\code{\link[dplyr:left_join]{dplyr::left_join}}>.}

\item{keep}{Passed to <\code{\link[dplyr:left_join]{dplyr::left_join}}>. Note that duplicate join key columns (\code{keep = TRUE}) are backfilled together like any other shared column, rather than kept separate.}

\item{.problem}{The problem number(s) to which the join will be applied. Uses all problems if \code{NULL}.}
}
\value{
An updated \code{xpose_data}/\code{xp_xtras} object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

<\code{\link[dplyr:left_join]{dplyr::left_join}}> wrapper for \code{xpose_data} (and, by inheritance,
\code{xp_xtras}) objects. Unlike a plain \code{left_join()}, a column present in
both \code{x} and \code{y} (other than the join keys) is not duplicated with
\code{.x}/\code{.y} suffixes: missing (\code{NA}) values already in \code{x} are backfilled
from the matching value in \code{y}, while non-missing values already in \code{x}
are left untouched. This makes it straightforward to backfill a variable
(or set of variables) that is only partially recorded, from a second data
source keyed on the same join variable(s) (e.g. \code{ID}).

\code{left_join_x()} accepts \code{xpose_data}/\code{xp_xtras} objects directly, with an
additional \code{.problem} argument restricting which problem(s) the join is
applied to.

\code{left_join()} without \verb{_x} is defined as an S3 method on \code{xpose_data}, so
that the usual <\code{\link[dplyr:left_join]{dplyr::left_join}}> generic dispatches here
automatically (\code{xp_xtras} objects are handled the same way, via class
inheritance).
}
\examples{
# Some subjects are missing an APGR score in the base dataset
xpdb_missing <- pheno_base \%>\%
  mutate_x(APGR = dplyr::if_else(ID \%in\% c("1", "2"), NA, APGR))

# A separate table with the (complete) values, keyed on ID
apgr_lookup <- xpose::get_data(pheno_base, quiet = TRUE) \%>\%
  dplyr::distinct(ID, APGR)

# Existing APGR values are kept; only the missing ones are filled in
left_join_x(xpdb_missing, apgr_lookup, by = "ID")

}
