% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{derive_shk}
\alias{derive_shk}
\alias{backfill_shk}
\title{Derive per-individual contribution to eta shrinkage}
\usage{
derive_shk(xpdb, ..., .problem = NULL, quiet)

backfill_shk(xpdb, ..., .problem = NULL, quiet)
}
\arguments{
\item{xpdb}{<\code{xpose_data}\link[xpose:xpose_data]{xpose::xpose_data}> or \code{xp_xtras} object}

\item{...}{<\code{tidyselect}> Which eta column(s) to derive a shrinkage
contribution for. Defaults to every \code{eta} column for \code{.problem}.}

\item{.problem}{<\code{numeric}> Problem number to use. Uses the xpose default if not provided.}

\item{quiet}{<\code{logical}> Silence extra debugging output}
}
\value{
For \code{derive_shk()}, a data frame with one new column per
selected eta, named \verb{<eta>_SHK}. For \code{backfill_shk()}, the updated
\code{xp_xtras} object, with those columns joined in and typed \code{shk}.
}
\description{
Computes, for each selected eta, a diagnostic column highlighting each
individual's contribution to shrinkage: \eqn{\log((\eta_i -
\bar\eta)^2)}, ie the log of the squared deviation from the population
mean eta. Since shrinkage itself is \code{100 * (1 - SD(eta)/omega)} (see
\code{\link[=recalc_shk]{recalc_shk()}}), and \code{SD(eta)^2} is the mean of these per-individual
squared deviations, this highlights which individuals are pulling
shrinkage down. The \code{log} spreads out values close to \code{0}, ie
individuals contributing the least (the most heavily shrunk).

\code{derive_shk()} returns the augmented data as a plain data frame, like
\code{\link[xpose:get_data]{xpose::get_data()}}'s output. \code{backfill_shk()} joins the new
column(s) back into \code{xpdb} and tags them with the \code{shk} variable type.
This has to be backfilled rather than parsed, since it isn't something
NONMEM (or any other supported software) reports directly.
}
\examples{
derive_shk(xpdb_x) \%>\%
  dplyr::select(ID, dplyr::ends_with("_SHK")) \%>\%
  head()

xpdb_x \%>\%
  backfill_shk() \%>\%
  list_vars()

}
