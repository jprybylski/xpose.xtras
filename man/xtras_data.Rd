% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xp_xtras.R
\name{xtras_data}
\alias{xtras_data}
\title{Read model outputs directly into an \code{xp_xtras} object}
\usage{
xtras_data(..., warn = FALSE)
}
\arguments{
\item{...}{Passed to \code{\link[xpose:xpose_data]{xpose::xpose_data()}}}

\item{warn}{<\code{logical}> If \code{FALSE} (default), \code{rlang_warning}-class
warnings raised while reading are silenced. If \code{TRUE}, all warnings
are passed through unmodified.}
}
\value{
An <\code{xp_xtras}> object
}
\description{
Convenience wrapper equivalent to
\code{xpose::xpose_data(...) \%>\% as_xp_xtras()}. \code{xpose::xpose_data()}'s
\code{quiet} argument already controls its own informative messages, but not
warnings raised while parsing NONMEM tables -- e.g. \code{readr} surfacing
every oddly formatted or \code{NaN}/\code{Inf} value it had to coerce, one
warning per table, which can drown out a warning that actually
matters. \code{dplyr} (>= 1.1.2, already required by this package) batches
any warnings raised inside a single \code{dplyr::mutate()} call -- which is
how \code{xpose} reads each table -- into one \code{rlang_warning} per call
rather than letting each one through individually.

By default, \code{xtras_data()} silences those (and only those) warnings;
pass \code{warn = TRUE} to see them as \code{xpose::xpose_data()} would raise
them. Warnings \code{xpose::xpose_data()} raises directly -- e.g. a table or
output file it couldn't find at all -- are unaffected either way, since
those indicate an actual problem rather than value-level noise.
}
\examples{
xtras_data(file = file.path(
  system.file("pheno_saemimp", package = "xpose.xtras"), "run18.lst"
))
}
\seealso{
\code{\link[xpose:xpose_data]{xpose::xpose_data()}}, \code{\link[=as_xpdb_x]{as_xpdb_x()}}
}
