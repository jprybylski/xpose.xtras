% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xtra_plots.R
\name{ind_plots_sample}
\alias{ind_plots_sample}
\title{Individual plots for a (stratified) sample of individuals}
\usage{
ind_plots_sample(
  xpdb,
  n = 9,
  stratify = NULL,
  seed = NULL,
  facets,
  .problem,
  quiet,
  ...
)
}
\arguments{
\item{xpdb}{<\code{xp_xtras}> or <\code{xpose_data}> object}

\item{n}{<\code{integer}> Number of individuals to sample. Defaults to 9. If
fewer individuals than \code{n} are available, all of them are used.}

\item{stratify}{<\code{tidyselect}> Optional column(s), other than the id
column, to stratify the sample by.}

\item{seed}{<\code{integer}> Optional seed, set (and restored on exit) for
reproducible sampling.}

\item{facets}{As in \code{\link[xpose:ind_plots]{ind_plots}}. Defaults to the id
column (and \code{stratify} column(s), if given) added to
\code{xpdb$xp_theme$facets}.}

\item{.problem}{<\code{numeric}> Problem number to use.}

\item{quiet}{<\code{logical}> Silence extra output.}

\item{...}{Passed on to \code{\link[xpose:ind_plots]{ind_plots}}}
}
\value{
The desired plot
}
\description{
A wrapper around \code{\link[xpose:ind_plots]{ind_plots}} that first draws a
sample of \code{n} individuals (9 by default, enough to fill a 3x3 page)
rather than plotting every individual in the dataset. If \code{stratify} is
provided, the sample is drawn proportionally from each level (or
combination of levels) of the \code{tidyselect}-ed column(s), so the sample
remains as representative as the data and \code{n} allow.
}
\details{
When \code{stratify} is used, the stratifying column(s) are appended to the
facet formula (in addition to the id column that \code{\link[xpose:ind_plots]{ind_plots}}
already facets by), so that the stratum each sampled individual belongs
to is visible in the plot.

Stratified sample sizes are allocated proportionally to stratum size
using the largest-remainder method, so the total sampled always equals
\verb{min(n, sum(individuals available across all strata))}.
}
\examples{
xpdb_x \%>\% ind_plots_sample(n = 6)
xpdb_x \%>\% ind_plots_sample(n = 6, stratify = SEX)
}
\seealso{
\code{\link[=ind_roc]{ind_roc()}}
}
