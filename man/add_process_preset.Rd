% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_preset.R
\name{add_process_preset}
\alias{add_process_preset}
\alias{process_preset}
\alias{print_process_preset}
\alias{remove_process_preset}
\alias{amend_process_preset}
\title{Add, apply, list, amend or remove \code{xpdb} processing presets}
\usage{
add_process_preset(
  preset,
  name = NULL,
  overwrite = FALSE,
  persist = FALSE,
  ask = TRUE,
  profile = "project"
)

process_preset(xpdb, preset, ...)

print_process_preset(name = NULL)

remove_process_preset(name, persist = FALSE, ask = TRUE, profile = "project")

amend_process_preset(
  name,
  preset,
  persist = FALSE,
  ask = TRUE,
  profile = "project"
)
}
\arguments{
\item{preset}{<\code{formula}> or <\code{function}> One-sided formula (e.g.
\code{~ .x \%>\% as_xpdb_x() \%>\% set_var_type(na = any_of(paste0("ETA", 5:9)))})
or a function taking an \code{xpdb} as its first argument. For
\code{process_preset()}, instead the \code{character} name or \code{numeric} (1-based)
index of a previously-added preset.}

\item{name}{<\code{character(1)}> Name to store/look up/amend the preset
under. For \code{add_process_preset()}, defaults to the next unused integer
(as a string) if omitted.}

\item{overwrite}{<\code{logical(1)}> If a preset already exists under \code{name},
should it be replaced? (default: \code{FALSE}, i.e. error)}

\item{persist}{<\code{logical(1)}> Write the resulting set of presets out to
a \code{.Rprofile} so they're available in future sessions too? See Details.
(default: \code{FALSE})}

\item{ask}{<\code{logical(1)}> When \code{persist = TRUE}, ask for interactive
confirmation before writing? (default: \code{TRUE}; only ever consulted when
\code{\link[rlang:is_interactive]{rlang::is_interactive()}} is already \code{TRUE})}

\item{profile}{<\code{character(1)}> Where to persist to when
\code{persist = TRUE}: \code{"project"} (default, \code{.Rprofile} in \code{\link[=getwd]{getwd()}}),
\code{"user"} (the user-level profile), or a literal file path.}

\item{xpdb}{<\code{\link[xpose:xpose_data]{xpose_data}}> or <\code{xp_xtras}> object
to apply the preset to}

\item{...}{For \code{process_preset()}, forwarded to the preset if it is a
function (ignored by formula presets, which only ever see \code{xpdb})}
}
\value{
\code{add_process_preset()}/\code{amend_process_preset()}/
\code{remove_process_preset()} return the (\code{character(1)}) preset name,
invisibly. \code{process_preset()} returns the processed \code{xpdb}.
\code{print_process_preset()} returns the printed presets (a named list),
invisibly.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

A "process preset" is a one-sided formula (using \code{.x} for the
incoming \code{xpdb}, as in a \code{purrr}-style lambda) or a plain function that
bundles up a repeated processing pipeline -- e.g. converting to
\code{xp_xtras}, dropping unused \code{ETA}s, assigning labels/levels -- so it can
be re-applied with \code{\link[=process_preset]{process_preset()}} instead of being retyped for every
model.

\code{add_process_preset()} stores a preset in the current session (by name,
or an auto-incrementing integer if \code{name} isn't given).
\code{process_preset()} applies a stored preset to an \code{xpdb}.
\code{print_process_preset()} lists stored presets. \code{remove_process_preset()}
deletes one. \code{amend_process_preset()} replaces an existing preset's
definition in place.
}
\section{Persistence and CRAN policy}{
By default, presets are session-only: they vanish when R restarts. Set
\code{persist = TRUE} to additionally write the current set of presets to a
\code{.Rprofile} so they're recreated automatically in future sessions.

Per CRAN policy, a package must not write to files outside
\code{\link[=tempdir]{tempdir()}} without the user's explicit, interactive consent, and never
as a side effect of a non-interactive process (\verb{R CMD check}, tests,
vignette builds, \code{Rscript}, ...). Accordingly, \code{persist = TRUE}:
\itemize{
\item only ever writes when \code{\link[rlang:is_interactive]{rlang::is_interactive()}} is \code{TRUE} -- it errors
otherwise, so it is always a no-op under \verb{R CMD check}/\code{testthat}/
\code{knitr} -- and
\item asks for confirmation (via \code{\link[utils:askYesNo]{utils::askYesNo()}}) before writing, unless
\code{ask = FALSE} is passed explicitly by the (already-interactive) caller.
}

The target file defaults to a \strong{project}-scoped \code{.Rprofile} (in
\code{\link[=getwd]{getwd()}}), which only affects R sessions started in that directory;
pass \code{profile = "user"} to instead target the user-level profile
(\code{Sys.getenv("R_PROFILE_USER")}, falling back to \verb{~/.Rprofile}), or any
string to use it as a literal file path. Presets are written as a single
marked block (bounded by \verb{# >>> xpose.xtras process presets ... >>>} /
\verb{# <<< ... <<<}) so re-syncing replaces the whole block rather than
accumulating duplicate calls, and the block is removed entirely once the
last preset is deleted. Persisted presets must be self-contained --
since they're recreated from deparsed source on each new session, they
cannot depend on transient local variables from the session that
created them.
}

\section{Formula presets and \code{.x} vs \code{.}}{
Use \code{.x} (not a bare \code{.}) as the placeholder for the incoming xpdb in a
formula preset, e.g. \code{~ .x \%>\% as_xpdb_x() \%>\% set_var_type(...)}. A
\emph{leading} \code{.} immediately before \verb{\%>\%} is itself magrittr syntax for
building a reusable function (see \code{?magrittr::`\%>\%`}): \code{~ . \%>\% f()}
would silently return a function instead of applying it, since the
formula's own \code{.} placeholder collides with magrittr's.
}

\examples{
add_process_preset(
  ~ .x \%>\% as_xpdb_x() \%>\% set_var_types(na = any_of(paste0("ETA", 5:9))),
  name = "drop_higher_etas"
)
print_process_preset()

xpdb_ex_pk_processed <- xpose::xpdb_ex_pk \%>\%
  process_preset("drop_higher_etas")

amend_process_preset(
  "drop_higher_etas",
  ~ .x \%>\% as_xpdb_x() \%>\% set_var_types(na = any_of(paste0("ETA", 7:9)))
)

remove_process_preset("drop_higher_etas")
}
\seealso{
\link[xpose:xpose_data]{xpose::xpose_data}
}
