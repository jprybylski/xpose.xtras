% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xtra_plot_all.R
\name{set_default_plots}
\alias{set_default_plots}
\title{Set a default plot spec on an \code{xp_xtras} object}
\usage{
set_default_plots(xpdb, plots)
}
\arguments{
\item{xpdb}{<\code{\link[xpose:xpose_data]{xpose_data}}> or <\code{xp_xtras}> object}

\item{plots}{A list of plot specs -- see \link[=plot.xpose_data]{plot.xpose_data()} for the
accepted forms}
}
\value{
\code{xp_xtras} object
}
\description{
Stores a plot spec on \code{xpdb} that \link[=plot.xpose_data]{plot.xpose_data()} uses instead of
the package's built-in default whenever \code{plots} isn't supplied directly
to that call. Unlike \code{\link[=set_default_labs]{set_default_labs()}}/\code{\link[=set_default_watermark]{set_default_watermark()}},
this replaces the \code{xpdb}-level default wholesale rather than merging
with it: entries in a plot spec aren't addressable by a stable key (the
same plot function can legitimately appear more than once), so there is
no sensible key-by-key merge to perform.
}
\examples{
xpdb2 <- set_default_plots(xpdb_x, list(~ xpose::dv_vs_ipred(.x), ~ xpose::eta_distrib(.x)))
names(plot(xpdb2, quiet = TRUE))
}
\seealso{
\code{\link[=set_xtras_options]{set_xtras_options()}} for the session-option equivalent
(\code{xpose.xtras.default_plots}), and \code{\link[=get_xtras_option]{get_xtras_option()}} to check which
one is currently dominant.
}
