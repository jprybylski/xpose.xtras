% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xtra_watermark.R
\name{add_watermark}
\alias{add_watermark}
\title{Add a watermark to a plot}
\usage{
add_watermark(plot, label, colour, alpha, size, angle, fontface, xpdb = NULL)
}
\arguments{
\item{plot}{<\code{ggplot}> or <\code{xpose_plot}> object}

\item{label}{<\code{character}> Watermark text}

\item{colour}{<\code{character}> Text colour, passed to \code{\link[grDevices:adjustcolor]{grDevices::adjustcolor()}}}

\item{alpha}{<\code{numeric}> Transparency of the watermark text, between 0
(invisible) and 1 (opaque)}

\item{size}{<\code{numeric}> Font size in points}

\item{angle}{<\code{numeric}> Rotation angle in degrees (counter-clockwise)}

\item{fontface}{<\code{character}> Font face, passed to \code{\link[grid:gpar]{grid::gpar()}}}

\item{xpdb}{<\code{\link[xpose:xpose_data]{xpose_data}}> or <\code{xp_xtras}> object
\code{plot} was built from, used to resolve \code{xpdb}-level defaults (see
\code{\link[=set_default_watermark]{set_default_watermark()}})}
}
\value{
\code{plot}, with the watermark layer added
}
\description{
Overlays large, semi-transparent, rotated text across a \code{ggplot} or
\code{xpose_plot} object (e.g. \code{"DRAFT"}, \code{"PRELIMINARY"}, \code{"CONFIDENTIAL"}).
Purely opt-in: call it on a plot when you want the watermark, there is
no automatic/global trigger.

\code{label}/\code{colour}/\code{alpha}/\code{size}/\code{angle}/\code{fontface} resolve with the same
precedence as \code{\link[=apply_default_labs]{apply_default_labs()}}: (in increasing precedence) the
\code{xpose.xtras.default_watermark} R option, \code{xpdb}-level defaults set via
\code{\link[=set_default_watermark]{set_default_watermark()}} (when \code{xpdb} is supplied), then the argument
itself when explicitly passed. Built-in fallbacks (\code{"DRAFT"},
\code{"grey50"}, \code{0.3}, \code{24}, \code{30}, \code{"bold"}) apply if a setting isn't
resolved from any of those.
}
\examples{
p <- xpose::dv_vs_ipred(xpose::xpdb_ex_pk)
add_watermark(p, label = "PRELIMINARY")

options(xpose.xtras.default_watermark = list(label = "CONFIDENTIAL", colour = "red"))
add_watermark(p)
options(xpose.xtras.default_watermark = NULL)
}
\seealso{
\code{\link[=set_xtras_options]{set_xtras_options()}} for the full list of \verb{xpose.xtras.*}
session options, and \code{\link[=get_xtras_option]{get_xtras_option()}} to check which tier
(option/\code{xpdb}) is currently dominant for a given \code{xpdb}.
}
