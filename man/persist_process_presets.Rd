% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_preset.R
\name{persist_process_presets}
\alias{persist_process_presets}
\title{Write current process presets out to a \code{.Rprofile}}
\usage{
persist_process_presets(ask = TRUE, profile = "project")
}
\arguments{
\item{ask}{<\code{logical(1)}> When \code{persist = TRUE}, ask for interactive
confirmation before writing? (default: \code{TRUE}; only ever consulted when
\code{\link[rlang:is_interactive]{rlang::is_interactive()}} is already \code{TRUE})}

\item{profile}{<\code{character(1)}> Where to persist to when
\code{persist = TRUE}: \code{"project"} (default, \code{.Rprofile} in \code{\link[=getwd]{getwd()}}),
\code{"user"} (the user-level profile), or a literal file path.}
}
\value{
\code{TRUE} if the file was written, \code{FALSE} if declined (invisibly)
}
\description{
Syncs the in-session \code{\link[=add_process_preset]{add_process_preset()}} registry out to a
\code{.Rprofile} file, so it's available again in future sessions. This is
what \code{persist = TRUE} on \code{\link[=add_process_preset]{add_process_preset()}}/\code{\link[=remove_process_preset]{remove_process_preset()}}/
\code{\link[=amend_process_preset]{amend_process_preset()}} calls internally; call it directly to persist
several in-session changes (each made with \code{persist = FALSE}) in one
write/confirmation instead of one per change. See the CRAN-policy notes
in \code{\link[=add_process_preset]{add_process_preset()}} -- in particular, this always errors instead
of writing anything when \code{\link[rlang:is_interactive]{rlang::is_interactive()}} is \code{FALSE}.
}
\examples{
\dontrun{
add_process_preset(~ .x \%>\% as_xpdb_x(), name = "convert", persist = FALSE)
persist_process_presets()
}
}
