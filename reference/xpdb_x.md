# An example `xp_xtras` object

The
\<[`xpdb_ex_pk`](https://uupharmacometrics.github.io/xpose/reference/xpdb_ex_pk.html)\>
object converted to `xp_xtras`. For examples.

## Usage

``` r
xpdb_x
```

## Format

### `xp_xtras`

An `xp_xtras` object with no extra data filled.

## Source

Assembled from the `xpdb_ex_pk` object in the `xpose` package.
